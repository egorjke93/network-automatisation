# Основные зависимости для SSH подключений
scrapli[ssh2]>=2024.1.30   # SSH подключение (основная библиотека для сбора данных)
netmiko>=4.0.0              # SSH для конфигурационных изменений (push descriptions)

# Парсинг вывода сетевых команд
ntc-templates>=6.0.0        # NTC Templates для парсинга (использует parse_output)

# Экспорт данных
openpyxl>=3.0.0             # Экспорт/импорт Excel
pandas>=2.0.0               # Работа с данными, сопоставление MAC

# NetBox интеграция (опционально)
pynetbox>=7.4.0             # NetBox API клиент (совместим с NetBox 4.x)

# Безопасность (опционально)
keyring>=24.0.0             # Хранение токенов в системном хранилище (Credential Manager/Keychain)
python-certifi-win32>=1.6.0 # SSL сертификаты из Windows Certificate Store

# Для scrapli ssh2 транспорта (рекомендуется)
ssh2-python>=1.0.0          # SSH2 транспорт для Scrapli (быстрее paramiko)
